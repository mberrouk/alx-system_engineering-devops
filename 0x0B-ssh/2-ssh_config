Host *                                # match hosts
    SendEnv LANG LC_*                 # specifies what environment variables should be sent to the server.
    HashKnownHosts yes                # ssh should hash host names and addresses (~/.ssh/known_hosts content). 
    GSSAPIAuthentication yes          # GSSAPI (for Kerberos authentication) is allowed.
    PasswordAuthentication no         # refuse to authenticate using a password. 
    IdentityFile ~/.ssh/school        # use the private key ~/.ssh/school.

